# web

Seller dashboard and buyer chat. Not built yet; see `CLAUDE.md` build order
items 2 and 4.

Talks to the API in `../api`. Nothing here should compute a price. Ask the
server, render what it returns.
